version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: store_backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      NODE_ENV: development
      PORT: 5000
    restart: unless-stopped